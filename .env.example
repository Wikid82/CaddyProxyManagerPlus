# CaddyProxyManager+ Environment Configuration
# Copy this file to .env and customize for your environment

# Server Configuration
SERVER_PORT=8080
DATA_PATH=./data

# Caddy Configuration
CADDY_ADMIN_URL=http://localhost:2019
CADDY_CONFIG_PATH=./config/Caddyfile

# Security
# Generate a secure JWT secret with: openssl rand -base64 32
JWT_SECRET=change-this-secret-in-production

# Database (SQLite)
# The database file will be created in DATA_PATH/caddyproxymanager.db

# CrowdSec (if using)
# CROWDSEC_API_URL=http://localhost:8080
# CROWDSEC_API_KEY=your-crowdsec-api-key

# Email (for Let's Encrypt notifications)
# LETSENCRYPT_EMAIL=admin@example.com

# Logging
# LOG_LEVEL=info
# LOG_FILE=/var/log/caddyproxymanager/app.log

# Optional: Custom Caddy binary path
# CADDY_BIN=/usr/bin/caddy

# Development Settings (do not use in production)
# DEBUG=false
# DEV_MODE=false
